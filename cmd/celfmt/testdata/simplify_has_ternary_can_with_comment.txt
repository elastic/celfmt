celfmt -s -i src.cel
! stderr .
cmp stdout want.txt

-- src.cel --
// comment 0
has(state.x.f) ?
  state.x.f
:
  // comment 1
  "default"
-- want.txt --
// comment 0
state.x.?f.orValue(
	// comment 1
	"default"
)
